

// Modules:
//

// Default Modules:
//
// They don't need to be given a name at the time of definition.

//@Example: movie.ts
export default class {
    title: string;
    director: string;
}

// @Exaple: Import default module into importModules.ts File.
//
// i.e. import AnimatedMovie from './modules';


// If an Exported Module extends a Class, Module or Another Object, 
// then that Object MUST be imported above the Export.